# Environment records and tested scope

The analysis used Python 3.12.14 with LIANA 1.10.0, NumPy 2.3.5, pandas 2.3.3 and SciPy 1.18.1; R 4.6.1 with Bioconductor 3.23 and decontX 1.10.0. These are actual recorded versions.

- [environment.yml](work/repro_v3_environment/environment.yml): conda base specification plus exact pip pins. Conda creation has not been tested; this is not a solved conda build lock.
- [109-package pip lock](work/repro_v3_environment/requirements-python.lock.txt) and [actual imported paths/version audit](work/repro_v3_environment/python_environment.json).
- [232-package renv.lock](work/repro_v3_environment/renv.lock), generated by renv 1.2.4 from installed DESCRIPTION records; [capture audit](work/repro_v3_environment/R_lock_capture_audit.json), [package sources](work/repro_v3_environment/R_package_descriptions.json), [sessionInfo](work/repro_v3_environment/R_sessionInfo.txt).
- [119-check environment verification](work/repro_v3_environment/environment_verification.json) and [bounded empty-library R restoration](work/repro_v3_environment/R_restore_smoke_audit.json). The latter restored renv/jsonlite with BiocManager bootstrap, not the entire scientific R stack.
- [Portable opt-in R restore script](work/repro_v3_environment/restore_r_environment.R) and [full commands, source caveats and installation boundaries](work/repro_v3_environment/report.md).

```text
conda env create -f work/repro_v3_environment/environment.yml
conda activate ischemia-lr-repro
python work/repro_v3_environment/verify_environment.py --release-root .
```

The create command is supplied for readers and remains untested here. No original runtime, local `.pth` link, machine-only artifact dependency or test-library binary is committed. Version pins and source metadata do not establish binary-identical restoration across platforms. R-universe source/build metadata are retained rather than being described as an unverified CRAN-only installation.

The second LIANA code path is fixed commit `d4211373692e7b9c10210488ccb1efe06452b097`, **aggregation functions only on archived scores**. Its [source provenance](work/repro_literature/liana_pr261_source_provenance.json), [upstream source](work/repro_literature/liana_merge_d421137_aggregate.py), [extracted functions](work/repro_literature/liana_merge_d421137_extracted_aggregation.py) and [22-network audit](outputs/reproducibility_v2/tables/rank_upstream_audit.json) are included. This scope must not be labeled a complete newer-version LIANA run.
