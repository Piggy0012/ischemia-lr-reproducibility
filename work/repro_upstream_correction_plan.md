# Fixed-commit upstream aggregation correction audit

2026-09-11 UTC. This descriptive amendment follows completion of the raw/native and unique-column diagnostics and discovery that upstream PR 261 had already merged on 2026-09-09. Its purpose is to attribute the existing correction accurately and determine how closely the separately implemented diagnostic matches the fixed upstream aggregation function. It introduces no hypothesis test and changes no original result or null family.

1. Preserve the public GitHub pull-request and merge-commit API responses, full merge SHA, exact new-path aggregation source and SHA256 hashes. Use scverse/liana commit d4211373692e7b9c10210488ccb1efe06452b097, not main, a moving tag, a redirect or a cached old repository path.
2. Extract only the four aggregation functions by Python AST from the inspected fixed source. Preserve their function bodies exactly. Supply NumPy/pandas/SciPy and a logging stub; do not install/import a new LIANA package. Annotate this as an aggregation-function-only execution.
3. Process the 22 saved raw full-network tables sequentially. Restore the three original magnitude score columns to float32, retain identical network rows, method specifications and target candidates, and re-evaluate the fixed upstream function. Compare native, earlier unique-column diagnostic and upstream aggregation. Reconstruct the earlier outputs first as a data/provenance check.
4. Check unique-column rank vectors and actual runtime dtypes directly. The new function constructs a rank matrix with column_stack, whereas the installed old function assigns ranks in place. Quantify actual output differences instead of declaring bitwise equivalence in advance. Separately pass a float64 copy of the same restored score values through both functions to isolate downstream rank-matrix precision without changing score ordering.
5. Report per-library full/target maximum absolute differences, bitwise equality, agreement at 10^-12 and 10^-6, and the source of numerical differences. On the unchanged fixed target sets (187/174), calculate descriptive cohort effects, both sign-concordance denominators, zero counts and rho for the upstream aggregation function. No new P values or transfer of original native P values.
6. Keep all inputs read-only. Record source hashes, exact extraction, versions and observed memory use; stop if the process peak exceeds 400 MiB. No corrected matrices, raw cell matrices, model fits, package installation or environment modification.

Interpretation is a documented known upstream correction and its cross-cohort consequences. The aggregation-function audit does not reproduce other merged changes, including expression-proportion defaults, and therefore cannot be described as a rerun of the complete newer LIANA pipeline or as discovery of a previously unknown bug.

Runtime clarification before final outputs: the first-library probe invalidated the initial code-reading assumption that column_stack necessarily made the new rank matrix float64. In this installed SciPy 1.18.1 environment, rankdata preserves float32 input precision; therefore both native-precision rank matrices are float32. The attempted float64-versus-float32 equality assertion was stopped, the probe was inspected, and the final protocol compares each precision consistently while also retaining the between-precision difference. This corrects a runtime assumption and does not change scores, ranking parameters, candidates or statistical endpoints.
