# Full-network rank implementation sensitivity

2026-09-11 UTC, after the minimal official-source diagnostic and before any real-network diagnostic result. Preserve all original native LIANA results. Rerun LIANA 1.10.0 separately for every animal with the original five-method default, full cell-type network, resource, 10% detection threshold, minimum 30 cells, normalization, and seed. Export the full result. For raw counts verify that the rerun's target scores and native ranks reproduce the saved earlier target export and that its context/row counts match its audit.

Using the same complete full-network score table, apply the installed official robust rank aggregation function once with the original specifications as a reproduction check and once with each distinct magnitude score column included only once. The second output is explicitly an implementation sensitivity, not an official patched LIANA release or an independent method. Preserve the source code hash and exact specifications. Do not modify the installed library. All full-network rows, target exports, original ranks, and diagnostic ranks are retained.

Primary selection for all 11 animals precedes reference_singlet for all 11 animals. Optional DecontX input uses the corrected all-gene counts in exactly the raw QC cell/gene order. Its logCP10K denominator is the sum of corrected counts over all genes for that cell. Abort on negative, nonfinite, or zero-total corrected cell counts rather than silently using raw totals. DecontX does not change the original cell identity/selection rules here. Corrected count source is labeled separately and is never compared to the raw stored scores as an equality check.

Real-data comparisons across native and unique-column diagnostic priority use fixed complete-case candidate sets for each count source and selection, all 11 animals and all relevant score metrics finite. Report both all-candidate and nonzero-direction concordance and rho. These follow-on results are descriptive, with no additional null tests or significance claims. Effects remain MCAO minus Sham across whole animal libraries. A change after this diagnostic can be attributed to this precise implementation intervention on the same full-network scores, but it does not validate the diagnostic as biological truth or establish general behavior across LR frameworks.
