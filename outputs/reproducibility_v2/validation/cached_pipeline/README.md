# Successful cached reconstruction evidence

All 12 selected cached stages completed on 2026-09-11. Scientific table values, schemas, row order, missingness and statistical definitions were unchanged; protected input and historical-test checksums were unchanged. The complete post-run validator passed with no pending stages. No raw/corrected single-cell matrices were opened and no model fitting, software installation, network access or figure regeneration occurred.

The plan, stage logs, before/after validation, protected-input checksums and full semantic comparison are included here. Byte changes in gzip containers and provenance JSON are retained in the comparison and are distinguished from scientific-value differences. The complete local before-output backup remains at the source run path recorded in evidence_manifest.json; its duplicate files are omitted from this compact archive evidence.

This is a same-workspace cached reconstruction. A separate relocated-stage reconstruction remains required for the portable-release check.
